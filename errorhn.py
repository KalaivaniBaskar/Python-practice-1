# try : try executing this, it may lead o error

# except : will be executed if there is an error in try block

# finally : this is a final block that is executed regardless of error

# you can have multiple except block with error types too
# except TypeError 
# except ImportError 
